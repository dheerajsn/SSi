# Core — always required
requests>=2.31.0
numpy>=1.24.0
faiss-cpu>=1.7.4
beautifulsoup4>=4.12.0
python-dotenv>=1.0.0
jinja2>=3.1.0
rank-bm25>=0.2.2

# Optional — local embeddings + reranking (sentence-transformers)
# Needed only for LocalEmbedder and LocalReranker.
# Skip if using OrchestraEmbedder / JinaEmbedder / JinaReranker instead.
# sentence-transformers>=2.7.0

# Optional — PDF ingestion
# Needed only for src/pdf_converter.py (converts PDFs to Azure DI JSON format).
# pdfplumber>=0.10.0
